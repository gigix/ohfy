<ul class="plans">
	<% current_user.plans.each do |plan| %>
		<li>
			<div class="plan_card">
				<h4><%= plan.title %></h4>
				<% plan.habits.each do |habit| %>
					<%= habit.summary %><br/>
				<% end %>
			</div>
		</li>
	<% end %>
</ul>